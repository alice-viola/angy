export function sendNotification(userId: string, message: string): Promise<void> {
  return Promise.resolve();
}
export function getUnreadCount(userId: string): Promise<number> {
  return Promise.resolve(0);
}
