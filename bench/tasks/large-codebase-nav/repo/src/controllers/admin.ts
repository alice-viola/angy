export function getSystemStats(): Promise<Record<string, number>> {
  return Promise.resolve({});
}
export function toggleFeatureFlag(flag: string, enabled: boolean): Promise<void> {
  return Promise.resolve();
}
