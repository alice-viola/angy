export function listProducts(categoryId?: string): Promise<unknown[]> {
  return Promise.resolve([]);
}
export function getProductDetail(slug: string): Promise<unknown | null> {
  return Promise.resolve(null);
}
