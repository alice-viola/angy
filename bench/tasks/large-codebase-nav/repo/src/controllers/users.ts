export function listUsers(page: number, limit: number): Promise<unknown[]> {
  return Promise.resolve([]);
}
export function getUserById(id: string): Promise<unknown | null> {
  return Promise.resolve(null);
}
