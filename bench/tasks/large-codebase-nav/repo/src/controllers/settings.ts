export function getUserSettings(userId: string): Promise<Record<string, unknown>> {
  return Promise.resolve({});
}
export function updateUserSettings(userId: string, settings: Record<string, unknown>): Promise<void> {
  return Promise.resolve();
}
